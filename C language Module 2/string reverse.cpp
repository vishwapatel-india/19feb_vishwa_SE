#include<stdio.h>
#include<string.h>
main()
{
	char str[10]; strrev[10];
	printf("Enter the string value here:");
	scanf("%s", &str);
	printf("\n the reversed string is:\n %s",strrev(str));
}
